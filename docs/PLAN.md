# Codex Continuity 实施计划

更新时间：2026-07-27

当前版本：v0.3.1

## v0.3.1 大会话与同步范围

- [x] 服务端改为 multipart 流式解析，密文直接写入数据目录，不再占用 64 MiB `/tmp`；
- [x] 最终加密包硬上限调整为 500 MiB，超过时客户端停止上传并给出缩小范围提示；
- [x] 客户端上传超时从 10 分钟调整为 30 分钟；
- [x] 设置页增加最近 2 天、5 天、一周、不限制；
- [x] 设置页增加项目多选，空选择状态表示全部项目；
- [x] 增加“包含已归档会话”开关，默认关闭；
- [x] 默认只同步最近一周，不再固定截断为最近 30 条；
- [x] 服务端请求日志记录 HTTP 状态码和响应大小；
- [ ] 分片续传、增量内容寻址和服务端容量保留策略。

## 产品目标

Codex Continuity 是独立于 Codex 的 Windows 常驻工具。它负责扫描固定工作区根目录关联的 Codex 会话，在本机加密后同步到私有服务器，并让另一台电脑获得可靠的继续入口。

服务端不访问 Codex、OpenAI、GitHub 或 GitLab。项目代码仍通过 Git 同步，Continuity 只同步会话快照、Git 状态摘要和必要的续接材料。

## v0.3.0 已完成

- [x] 桌面端收敛为“会话 / 同步 / 设置”三层导航；
- [x] 扫描 `~/.codex/sessions` 和 `session_index.jsonl`，按工作区根目录关联会话；
- [x] 会话按项目折叠分组，默认显示最近一周且每个项目先显示最近 4 条；
- [x] 归档会话暂不扫描、不上传，历史服务端快照不自动删除；
- [x] 会话搜索、状态筛选、详情联动和“在此设备继续”；
- [x] 本机会话原生定位与远端会话安全上下文续接；
- [x] 每 5 分钟后台检查，只有会话指纹变化时创建新快照；
- [x] 手动“立即同步”和 `Ctrl+Alt+P`；
- [x] 上传失败后持久化 `.ccx + .json` 离线队列；
- [x] 网络恢复后自动清空离线队列；
- [x] 加密归档导入、导出；
- [x] 托盘右键快捷窗口和最近会话入口；
- [x] Windows Credential Manager 保存令牌与共享密钥；
- [x] 连接测试与 64 KiB 加密上传测试；
- [x] 蓝色、青绿、紫色主题；
- [x] Docker Compose 服务端；
- [x] 用户登录、成员隔离、设备、API 令牌、会话快照和客户端下载管理；
- [x] AES-256-GCM 分块加密，服务端只存密文；
- [x] NSIS、MSI、便携版和 Linux 服务端构建流程；
- [x] React、Rust、Go 构建与单元测试；
- [x] 桌面主窗口、托盘窗口和服务端登录页浏览器交互验证。

## v0.3.x 稳定性工作

- [ ] 对单个超大快照增加可恢复的分片续传；
- [ ] 增量内容寻址，减少同一大会话重复上传；
- [ ] 远端快照保留、容量配额和自动清理策略；
- [ ] 设备撤销、最后心跳详情和远程退出；
- [ ] 可配置项目排除与敏感目录规则；
- [ ] GitHub/GitLab 提交可达性检查；
- [ ] dirty 工作树续接前冲突检查；
- [ ] Windows 系统通知；
- [ ] 更完整的审计日志和 API 限流。

## 商用发布前必须完成

- [ ] HTTPS 或企业私有 VPN；
- [ ] 修改默认管理员密码；
- [ ] Windows OV/EV 代码签名；
- [ ] 自动升级清单签名、灰度与回滚；
- [ ] 数据备份和恢复演练；
- [ ] 500 MiB 大包、断网、磁盘满和进程崩溃测试；
- [ ] 多用户越权与密钥轮换安全审计；
- [ ] Windows 10/11、Intune、SCCM 和企业镜像兼容测试；
- [ ] 公司数据出网与源代码合规确认。

## 明确不做

- 不修改 Codex 内部 SQLite 或任务数据库；
- 不承诺把一台电脑上的原生任务 ID 原样导入另一台电脑；
- 不自动覆盖目标工作区；
- 不替代 GitHub/GitLab 的代码同步；
- 不把 Skill、MCP 或 Plugin 作为同步主链路。
